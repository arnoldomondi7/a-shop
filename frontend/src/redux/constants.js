//set the the base url
//for both development and production.
//in production it will be the same domain
//without the use of localhost.
export const BASE_URL = "" //because the proxy handled it.
export const PRODUCT_URL = "/api/products" //url for the product route
export const USERS_URL = "/api/users" //url for the users route
export const ORDERS_URL = "/api/orders" //url for the order routes
export const PAYPAL_URL = "/api/config/paypal" //url for the paypal route
